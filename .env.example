# 应用密钥（随机生成，用于 session 签名）
SECRET_KEY=change-me-to-a-random-32-char-string

# 管理员账号
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me-strong-password

# Fernet 加密密钥（用于 SMTP 密码加密，运行 python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())" 生成）
FERNET_KEY=change-me-fernet-key

# 阿里云通义千问 API Key（用于 AI 摘要生成，可选，留空则使用简单文本提取）
# 申请地址：https://dashscope.console.aliyun.com/
DASHSCOPE_API_KEY=

# 数据库路径
DATABASE_URL=sqlite+aiosqlite:///./data/app.db

# 日志级别
LOG_LEVEL=INFO
